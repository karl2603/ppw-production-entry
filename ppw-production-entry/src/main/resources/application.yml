spring:

  application:
    name: apexflow-production

  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/apexflow_production?useSSL=false&serverTimezone=UTC}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:"Burlbed58#"}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: ${SERVER_PORT:8080}

app:

  jwt:
    secret: ${JWT_SECRET:bXktZGV2LWFwZXhmbG93LXNlY3JldC1rZXktbXVzdC1iZS1sb25nLWVub3VnaC1mb3ItaHMyNTYtYW5kLWxvY2FsLWRldg==}
    expiration-minutes: 480

  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173}